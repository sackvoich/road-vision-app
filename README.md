# Road Vision App 👁️🚗

Прототип приложения для распознавания дорожных знаков и разметки (пешеходных переходов) в реальном времени с видеопотока.

**Статус проекта:** `В разработке (MVP / Прототип)`

## ✨ Основные возможности

*   **Детекция дорожных знаков:** Распознавание **155 классов** дорожных знаков с помощью модели на базе YOLO11.
*   **Сегментация разметки:** Обнаружение и выделение контуров пешеходных переходов («зебр») с помощью сегментационной модели YOLO11-Seg.
*   **Обработка в реальном времени:** Прием видеопотока с клиента, параллельная обработка двумя моделями и отправка результатов обратно через **WebSockets**.
*   **Асинхронный бэкенд:** Высокопроизводительный сервер на **FastAPI**, способный обрабатывать ML-задачи без блокировки основного потока.

## 🛠️ Стек технологий

*   **Бэкенд:** Python 3.11+, FastAPI, Uvicorn
*   **Машинное обучение:** PyTorch, Ultralytics (YOLOv8)
*   **Веб-технологии:** WebSockets, HTML5, CSS3, JavaScript
*   **Инструменты:** Git, Git LFS

## 🚀 Как запустить проект

Для запуска бэкенда на локальной машине выполните следующие шаги.

### Предварительные требования

1.  **Python 3.11+**: Убедитесь, что у вас установлена соответствующая версия Python.
2.  **Git**: [https://git-scm.com/downloads](https://git-scm.com/downloads)
3.  **Git LFS**: **(Обязательно!)** Так как модели хранятся с помощью LFS, его необходимо установить.
    ```bash
    # Установка (один раз для системы)
    git lfs install
    ```

### Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/sackvoich/road-vision-app.git
    cd road-vision-app
    ```

2.  **Загрузите файлы моделей:**
    Эта команда скачает актуальные версии моделей из LFS-хранилища.
    ```bash
    git lfs pull
    ```

3.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Создание
    python -m venv venv

    # Активация (Windows)
    .\venv\Scripts\activate

    # Активация (macOS/Linux)
    source venv/bin/activate
    ```

4.  **Установите зависимости:**
    Проект использует `uv` для управления зависимостями. Если у вас его нет, установите: `pip install uv`.
    ```bash
    uv pip sync
    ```
    *Альтернативный вариант (если сгенерировать `requirements.txt`):*
    ```bash
    pip install -r requirements.txt
    ```

5.  **Запустите сервер:**
    ```bash
    uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ```

6.  **Откройте приложение:**
    Перейдите в браузере по адресу [http://127.0.0.1:8000](http://127.0.0.1:8000). Вы должны увидеть веб-интерфейс для тестирования.

## 📁 Структура проекта

```
.
├── .gitattributes      # Конфигурация для Git LFS
├── .gitignore          # Файлы и папки, игнорируемые Git
├── main.py             # Основной файл FastAPI-приложения (эндпоинты, логика)
├── pyproject.toml      # Определение зависимостей и проекта
├── uv.lock             # Файл блокировки версий зависимостей
├── README.md           # Этот файл
|
├── models/             # Директория для обученных моделей
│   ├── traffic_signs_detection_model.pt
│   └── zebra_segmentation_model.pt
|
└── static/             # Статические файлы для фронтенда
    └── app.js          # Клиентская логика на JavaScript
```

---
